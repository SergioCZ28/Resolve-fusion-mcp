# Pre-commit hooks. Run `pip install pre-commit && pre-commit install`
# after cloning so the hooks run on every `git commit`.
# https://pre-commit.com
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.21.2
    hooks:
      - id: gitleaks
